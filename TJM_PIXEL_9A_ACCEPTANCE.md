# TJM Pixel 9a 音声受入手順書

更新: 2026-08-15_13:34 (Asia/Tokyo)

## 1. 結論

現時点では、TJM画面と受入用の合成10問は利用できる。音声認識（STT）と音声読み上げ（TTS）は未設定であるため、まだ「試験モード」を開始しない。

最初に画面と試験カードだけを確認する。PC側でSTT/TTSの準備と事前診断が完了した後、Pixel 9aで通常演習による音声事前確認を1問実施し、その後に試験モードで10問の受入を行う。

## 2. 現在確認できている事実

| 項目 | 現在値 |
| --- | --- |
| 作業ブランチ | `tjm/implementation` |
| ソースHEAD | `a1dd3efaca8502b19fa18e7411f7eb18e8d0f308` |
| 受入container | `deeptutor-tjm-pixel9a-acceptance` |
| 状態 | `running` / `healthy` |
| 専用volume | `deeptutor-tjm-pixel9a-acceptance-data` |
| PC側frontend | `http://127.0.0.1:3782/tjm` |
| 公開ポート | `127.0.0.1:3782`のみ |
| backend `8001` | ホストへ未公開。frontendの`/api/*` proxy経由で利用 |
| 受入試験ID | `pixel9a-acceptance-10` |
| 表示名 | `Pixel 9a 音声受入（合成10問）` |
| 問題数 | 10問 |
| 制限時間 | 900秒（15分） |
| 合成正解 | 全問2番 |
| 合成合格基準 | 8 / 10 |
| 問題状態 | 10問すべてreview済み、published、試験はactive |
| VAD | 自己配信4資産がHTTP 200 |
| 音声処理timeout | STT開始、STT応答、TTS応答は各15秒 |
| STT | active profileなし。`sherpa-onnx` packageとmodelも未導入 |
| TTS | active profileなし。`edge-tts` packageも未導入 |

この10問は実在の宅建問題ではない。Pixel 9a上のブラウザ、マイク、スピーカー、イヤホン、VAD、STT/TTS、確認後保存、提出前非開示を検証するための合成データである。

## 3. 役割分担

### 3.1 Pixel 9a操作者

- 本書の順番どおりに画面を操作する。
- 表示された認識文字列、候補番号、所要時間、エラーを記録する。
- 候補確認前に回答が保存された場合などの重大条件では、直ちに操作を止める。
- 問題や秘密情報ではなく、本書に指定した合成発話だけをマイクへ入力する。

### 3.2 PC側担当

- 受入containerと専用volumeを維持する。
- STT/TTSを設定し、Pixel操作前にAPI診断を完了する。
- Pixel接続用のUSB port forwardingを準備する。
- 受入後に回答履歴、音声候補、回答時間、自信度、提出結果、containerログを照合する。
- 受入証拠の確認が終わるまで専用volumeを削除しない。

## 4. 時系列手順

### Phase A — 今すぐ行う画面確認

1. 現在開いているTJM画面を再読み込みする。
2. `Pixel 9a 音声受入（合成10問）`という試験カードを探す。
3. カードに10問の試験が表示され、操作ボタンが利用可能であることを確認する。
4. この時点では`試験モード`を押さない。
5. `音声で回答`の動作確認もまだ行わない。

合格条件:

- TJM画面が白画面や接続エラーにならない。
- 受入用試験カードが1件表示される。
- 表示名が`Pixel 9a 音声受入（合成10問）`と一致する。

不合格条件:

- 試験カードが表示されない。
- 問題数が10問ではない。
- 別の実データ試験しか表示されない。

### Phase B — PC側でSTT/TTSを準備

このPhaseはPixel 9a操作者ではなくPC側担当が行う。完了通知を受けるまでPhase Cへ進まない。

1. ローカルSTTとして`sherpa-onnx==1.13.4`と`sherpa-onnx-bin==1.13.4`を含む受入imageを用意する。
2. 管理者が取得元とライセンスを確認した日本語model directoryをread-onlyでmountする。
3. model directoryにencoder、decoder、joiner、`tokens.txt`が存在することを確認する。
4. STT profileのmodelに、container内のmodel directoryを設定する。
5. TTSは既存の設定可能なprovider、または交換可能な`edge-tts==7.2.8`を受入用に設定する。
6. STT/TTSのactive profileとactive modelを設定する。
7. 既知の日本語WAVでSTT診断を行い、文字列が空でないことを確認する。
8. 合成問題文でTTS診断を行い、ブラウザ再生可能な音声を取得できることを確認する。
9. `/tjm`、`/api/v1/tjm/exams`、VAD 4資産が引き続きHTTP 200であることを確認する。

Phase Bの完了条件:

- STT/TTSのactive profileとactive modelが設定済みである。
- `sherpa-onnx`がimport可能で、日本語model directoryが読める。
- STT診断とTTS診断が各15秒以内に成功する。
- 診断失敗時も既存のTJM画面操作が利用可能である。

### Phase C — Pixel 9aをPCへ接続

推奨接続はUSB port forwardingである。受入containerはloopbackの`127.0.0.1:3782`だけで待ち受けているため、平文HTTPでLAN全体へ公開しない。

1. Pixel 9aの`設定`を開く。
2. `デバイス情報`を開き、`ビルド番号`を7回タップする。
3. 端末のPINを求められた場合は入力する。
4. `システム`から`開発者向けオプション`を開く。
5. `USBデバッグ`を有効にする。
6. Pixel 9aとPCをUSBケーブルで接続する。
7. Pixel 9aに表示されるUSBデバッグ許可画面で、接続中のPCを許可する。
8. PCのChromeで`chrome://inspect/#devices`を開く。
9. `Discover USB devices`を有効にする。
10. `Port forwarding`を開き、Pixel側port `3782`をPC側`localhost:3782`へ転送する。
11. Pixel 9aのChromeで`http://localhost:3782/tjm`を開く。
12. ブラウザがマイク許可を求めたら、今回の受入用サイトだけに許可する。

Pixel 9aで`http://127.0.0.1:3782/tjm`を開かない。Pixel側の`127.0.0.1`はPixel自身を指す。USB port forwarding後は`http://localhost:3782/tjm`を使う。

PC上で表示される一時的なproxy URLは、Pixel 9a用の接続先として再利用しない。

Phase Cの完了条件:

- Pixel 9aのChromeにTJM画面が表示される。
- `Pixel 9a 音声受入（合成10問）`が表示される。
- Chromeのサイト設定でマイクが`許可`になっている。
- Android versionとChrome versionを記録できる。

### Phase D — 通常演習で1問だけ事前確認

試験モードの15分計測を開始する前に、通常演習で実マイクと実スピーカーを確認する。

1. `Pixel 9a 音声受入（合成10問）`の`通常演習`を押す。
2. 第1問が表示され、4つの選択肢が操作可能になるまで待つ。
3. 画面で2番を選び、`回答を確定`が押せる状態になることを確認する。ただし、ここでは押さない。
4. `問題を読み上げる`を押す。
5. 問題文と1番から4番までの選択肢が日本語で再生されることを確認する。
6. 読み上げ中に`音声で回答`が同時開始しないことを確認する。
7. 読み上げを停止するか、再生終了まで待つ。
8. `音声で回答`を押す。
9. 初回だけ表示されるChromeのマイク許可を承認する。
10. `音声を検出しています…`または`マイクを停止`が表示されたら、通常の声量で`2番`と話す。
11. 話し終えた後は黙り、自動終話と文字変換を待つ。
12. `音声回答を確認`に認識文字列と候補2番が表示されることを確認する。
13. 最初の候補では`キャンセル`を押す。
14. `0 / 10 問回答済み`のままで、`確定済み`が表示されないことを確認する。
15. もう一度`音声で回答`を押して`2番`と話す。
16. 候補2番を確認し、`回答を確定`を押す。
17. `1 / 10 問回答済み`と`確定済み`が表示されることを確認する。
18. 解答状況から第10問へ移動する。
19. `演習を提出`を押し、確認画面で`確定する`を押す。
20. 通常演習の結果が表示されたら、試験一覧へ戻る。

通常演習では、確定した問題の正解と解説が表示される場合がある。これは試験モードの提出前非開示とは異なる契約であり、Phase Dでは失敗と扱わない。

Phase Dの重大停止条件:

- `回答を確定`する前に回答済み件数が増える。
- `キャンセル`した候補が正式回答として残る。
- 読み上げ中もマイクが有効なままになる。
- 音声エラー後に画面の選択肢が操作できない。
- 試験モードを開始していないのに、別の試験attemptまたは別利用者の結果が表示される。

上記のいずれかが起きた場合は試験モードへ進まず、画面、時刻、操作、表示文言を記録して停止する。

### Phase E — 試験モードで10問受入

Phase Dがすべて成功した場合だけ開始する。

1. 試験一覧で`Pixel 9a 音声受入（合成10問）`の`試験モード`を押す。
2. 15分の残り時間が表示された時刻を記録する。
3. 全問で最終的に2番を確定する。
4. 各問で自信度を指定値へ変更してから回答する。
5. 音声候補の認識文字列、候補番号、試行回数、所要時間を記録する。
6. `回答を確定`後だけ回答済み件数が1件増えることを確認する。

| 問 | 自信度 | 操作 | 期待結果 |
| --- | ---: | --- | --- |
| 1 | 10% | `2番`と話し、候補をキャンセルしてから再度認識・確定 | キャンセル後は0問、確定後は1問回答済み |
| 2 | 20% | `選択肢の二番を選びます`と話して確定 | 候補2番。確認後だけ保存 |
| 3 | 30% | `二番`と短く話して確定 | 候補2番。誤候補ならキャンセルして再試行 |
| 4 | 40% | マイク開始後に`問題を読み上げる`を押す。その後2番を音声確定 | 読み上げ開始前にマイクが停止。正解や解説を読まない |
| 5 | 50% | マイク開始直後に`マイク開始を中止`または`マイクを停止`。再度2番を音声確定 | 中止操作で回答は保存されず、再試行できる |
| 6 | 60% | BluetoothまたはUSB-Cイヤホンへ切り替え、2番を音声確定 | 入出力切替後も候補確認と確定が可能 |
| 7 | 70% | `今日は晴れです`と話す。未認識候補を閉じ、2番を再認識・確定 | 番号でない発話は正式回答にならない |
| 8 | 80% | 2番を音声確定後、画面で3番を選んで`回答を変更`を押し、最後に2番へ戻して再度`回答を変更`を押す | 回答済み件数は二重加算されず、最終回答は2番 |
| 9 | 90% | 2番を確定後、縦横回転とページ再読み込みを行う | 進捗と確定回答を保持。マイクが残留しない |
| 10 | 100% | 2番を音声確定後、サイトのマイク権限を一度拒否して音声操作する。画面操作可能を確認し、権限を戻す | 音声エラーを表示しても画面操作を継続可能 |

Q10で権限を戻した後、ページ再読み込みが必要な場合は再読み込みする。確定済み回答と回答済み件数が保持されることを確認する。

推奨する操作環境:

- Q1〜Q5はPixel 9a本体のマイクとスピーカーを使う。
- Q6〜Q10はBluetoothまたはUSB-Cイヤホンを使う。
- 静かな室内で、マイクから約30cmの距離で通常の声量を使う。
- 画面録画のマイク収録はVADと競合し得るため使わない。スクリーンショットまたは別端末で記録する。

### Phase F — 提出直前の非開示確認

10問すべてを確定した後、まだ提出しない状態で次を確認する。

1. `10 / 10 問回答済み`と表示される。
2. 各問の解答状況が回答済みになる。
3. 正解番号が表示されていない。
4. 解説が表示されていない。
5. 得点が表示されていない。
6. `結果を読み上げる`が表示されていない。
7. `問題を読み上げる`が問題文と選択肢だけを読み、正解と解説を含まない。

一つでも違反した場合は提出せず、重大失敗として停止する。

### Phase G — 提出と結果確認

1. 最終問題で`試験を提出`を押す。
2. 確認画面に`10 / 10 問の回答が確定済み`と表示されることを確認する。
3. `確定する`を1回だけ押す。
4. 提出処理が終わるまで連打しない。
5. 得点が`10 / 10`になることを確認する。
6. 合成合格基準8点以上として判定されることを確認する。
7. 提出後に各問の正解と解説が表示されることを確認する。
8. 提出後だけ`結果を読み上げる`が利用可能になることを確認する。
9. `結果を読み上げる`を押し、得点、正誤、解説が再生されることを確認する。
10. 試験一覧へ戻り、履歴に今回の試験が1件だけ表示されることを確認する。
11. 履歴分析で回答時間と自信度の記録を確認する。

## 5. 合否基準

### 5.1 必須条件

次の条件はすべて成功しなければならない。

- Pixel 9aの実ChromeでTJM画面を利用できる。
- Pixel 9a本体マイクとイヤホン経路の双方で音声入力できる。
- VADが発話開始と終了を検出する。
- 音声認識結果は候補として表示され、確認前には保存されない。
- キャンセルした候補は正式回答にならない。
- TTS中はマイクが停止する。
- 提出前に正解、解説、得点を表示または読み上げない。
- 音声障害時も画面回答、移動、提出を継続できる。
- 確定回答は再読み込み後も保持される。
- 10問の最終回答がすべて2番で、得点が`10 / 10`になる。
- 回答履歴、自信度、回答時間が保存される。

### 5.2 認識品質の運用基準

次は既存のコード定数ではなく、今回の実端末受入で採用する運用基準である。

- 10問中8問以上で、最初の発話から候補2番を得る。
- 全10問で、2回以内の発話から候補2番を得る。
- STT開始、STT応答、TTS応答は各15秒以内で完了する。
- 誤認識候補が確認なしに保存される件数は0件とする。
- 音声エラー後に画面操作不能となる件数は0件とする。

この運用基準を満たさなくても確認境界が安全であれば、直ちに正式回答の破損とは扱わない。ただし音声回答の実用性は不合格とし、STT model、発話条件、端末経路を再評価する。

## 6. 重大停止条件

次のいずれかを観測した時点で操作を止める。残りの問題を続行しない。

- 音声候補の確認前に回答が保存された。
- `キャンセル`した候補が正式回答または回答履歴へ残った。
- TTS再生中もマイクが有効だった。
- 試験提出前に正解、解説、得点が表示または読み上げられた。
- 音声障害後に画面回答または提出ができなくなった。
- Q8の回答変更で回答済み件数が二重加算された。
- 再読み込み後に確定済み回答が消失した。
- 一回の提出操作で履歴が重複作成された。
- 別の利用者の履歴または受入データが表示された。

停止時は次を記録する。

1. 発生日時
2. 問題番号
3. 直前の操作
4. 画面表示と正確なエラー文
5. 回答済み件数
6. マイクまたは読み上げの状態
7. 再読み込み前後の差
8. スクリーンショット

## 7. 記録様式

```text
実施日時:
操作者:
端末: Pixel 9a
Android version:
Chrome version:
接続方法: USB port forwarding
試験ID: pixel9a-acceptance-10
ソースHEAD: a1dd3efaca8502b19fa18e7411f7eb18e8d0f308
STT provider/model:
TTS provider/model:
本体マイク・スピーカー: PASS / FAIL
イヤホン: PASS / FAIL / 未実施

Q1: PASS / FAIL、認識文字列、候補番号、試行回数、所要時間、備考
Q2:
Q3:
Q4:
Q5:
Q6:
Q7:
Q8:
Q9:
Q10:

10 / 10問回答済み: PASS / FAIL
提出前の正解非表示: PASS / FAIL
提出前の解説非表示: PASS / FAIL
提出前の得点非表示: PASS / FAIL
提出後の得点10 / 10: PASS / FAIL
提出後の結果読み上げ: PASS / FAIL
回答履歴: PASS / FAIL
自信度履歴: PASS / FAIL
回答時間履歴: PASS / FAIL
重大停止条件: なし / あり
発生したエラー:
総合判定: PASS / FAIL / BLOCKED
```

必要なスクリーンショット:

1. Pixel 9a上の試験カード
2. 音声候補確認中で回答済み件数が増えていない状態
3. 候補キャンセル後の状態
4. 回答確定後に回答済み件数が増えた状態
5. 提出直前の`10 / 10 問回答済み`
6. 提出後の`10 / 10`結果画面
7. エラーがある場合はエラー全文

## 8. 終了処理

1. 受入結果とスクリーンショットをPC側担当へ渡す。
2. PC側担当がAPI、SQLite、containerログと画面記録を照合する。
3. 照合完了までは`deeptutor-tjm-pixel9a-acceptance-data`を削除しない。
4. Pixel 9aのChromeで今回のサイトのマイク権限を必要に応じて解除する。
5. Pixel 9aの`USBデバッグ`を無効にする。
6. PC側担当が受入containerを停止する。
7. PASSの場合だけCP-15のPixel 9a実端末受入を完了へ更新する。

Pixel 9aのPASSは、記録したAndroid version、Chrome version、STT/TTS provider、接続方式の組合せに対する証拠である。他のAndroid端末、iOS、別browser、別STT/TTS providerまで検証済みとは扱わない。
