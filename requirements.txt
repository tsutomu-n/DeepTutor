# ============================================
# DeepTutor Python Dependencies (Runtime Bundle)
# ============================================
# Python >= 3.11,<3.14 required
#
# Single source of truth: pyproject.toml `[project.optional-dependencies]`.
# Files under requirements/ mirror those extras for Docker/CI installs that
# don't yet have access to pyproject.toml + source code.
#
# JustPass private-checkout source installs:
#   python -m pip install -e .                    # Full app
#   python -m pip install -e ./packaging/deeptutor-cli  # CLI-only
#   python -m pip install -e ".[partners]"       # Server + partner channel SDKs
#   python -m pip install -e ".[matrix]"         # Matrix channel for partners
#   python -m pip install -e ".[math-animator]"  # Manim animation engine (addon)
#   python -m pip install -e ".[dev]"            # Server + dev/test tools
#   python -m pip install -e ".[all]"            # Everything
#
# This file installs the full server runtime (CLI + API + partners).

-r requirements/partners.txt
